# Herdr agent id -> sidebar icon.
#
#   @name    glyph from the generated AgentIcons font (logos/name.svg)
#   <char>   any literal text (emoji, nerd font glyph, letter)
#
# Agent ids are Herdr's canonical ids — see `herdr agent list`.
# Add a logos/<name>.svg, run `uv run tools/build-font.py`, then reference
# it here as @name. No other code changes needed.

agy             @agy
devin           @devin
droid           @droid
codex           @codex
claude          @claude
cmd             @cmd
pi              @pi
amp             @amp
gemini          @gemini
cursor          @cursor
github_copilot  @github_copilot
grok            @grok
hermes          @hermes
kilo            @kilo
kimi            @kimi
open_code       @open_code
qodercli        @qodercli
qwen            @qwen
deepseek        @deepseek
ollama          @ollama
mistral         @mistral
openai          @openai

# No official vector logo found — emoji fallback:
letta           🧠
muse            🎨
maki            🍣
kiro            👻
